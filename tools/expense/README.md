# Expense Tracker（生活账单）

纯前端个人记账工具，数据存储在浏览器本地 IndexedDB 中，无需注册、无需联网即可使用。

## 功能概览

- **📊 概览面板**：大号 Hero 金额展示（本月支出 + 趋势对比），全局筛选联动节奏提醒、日历热力图、值得留意的小变化、分类占比、TOP 5 和支出趋势
- **➕ 快捷记账**：表单模式 + 自然语言模式双入口，今日小账本快捷项、智能标签建议、最近标签组合模板
- **🌿 产品欢迎页**：温柔生活账本首屏，支持记第一笔、导入 Excel / CSV、体验演示数据，并强调本地隐私和离线使用
- **📋 账单明细**：日期分组展示，搜索/分类多选/排序筛选，分页加载，内联编辑与删除
- **🏷️ 标签管理**：一级分组 + 二级标签层级体系，树状可折叠管理，颜色自定义，分组间移动标签，重命名/合并/删除
- **📥 数据导入**：CSV / Excel (.xlsx) 拖拽或选择导入，全字段可编辑预览，按行删除，批量清理无效记录，字段自动映射
- **📤 数据导出**：CSV 表格导出、JSON 完整备份
- **⚙️ 设置**：演示模式切换、数据清除、浅色/深色/跟随系统主题、问题反馈入口、PWA 离线支持
- **📱 移动端适配**：4 项底部导航 + 突出但不悬浮的记账入口，安全区域适配，滚动收缩 Header，触控优化

---

## 版本历史

### Unreleased

- **生活化视觉升级**：欢迎页和空状态加入温柔桌面账本视觉资产，强化女性友好、日常记录和安心收纳的氛围。
- **今日小账本**：记账页新增咖啡、午餐、地铁、护肤快捷项，一键填入金额、项目和常用标签。
- **空状态引导优化**：概览空状态改为“开始你的本月账本”，提供记第一笔和演示数据入口，并说明记录 3 天后的价值。
- **离线资产缓存**：新视觉资产进入生活账单 Service Worker 预缓存，安装到主屏幕后也能保持完整视觉。
- **回归测试**：新增静态断言覆盖快捷入口、概览提醒列表唯一 id 和视觉资产缓存。

### v1.6.10 (2026-07-07)

- **标签筛选展示优化**：概览页和账单明细页的已选标签按分组聚合展示。
- **减少误触**：概览页已选标签不再显示快捷删除按钮，统一通过标签面板修改。
- **明细页可读性**：分类筛选按钮直接展示选中的标签内容，不再只显示数量。
- **版本号更新**：设置页版本号更新为 `v1.6.10`

### v1.6.7 (2026-07-02)

- **分类多选筛选**：账单明细分类筛选改为自定义多选面板，可一次选择多个标签。
- **分组折叠**：分类筛选按标签分组展示，支持折叠分组快速定位。
- **版本号更新**：设置页版本号更新为 `v1.6.7`

### v1.6.6 (2026-07-01)

- **花费提醒**：替换原支出节奏百分比条，直接说明本期已花、比参考进度多/少、日均和剩余天数。
- **长范围热力图**：选择今年等较长时间范围时自动按月汇总，避免日历格过长。
- **明细分类筛选**：按标签分组展示筛选项，并用稳定标签 id 筛选，兼容旧分类名称。
- **版本号更新**：设置页版本号更新为 `v1.6.6`

### v1.6.5 (2026-07-01)

- **日历热力图**：改为更紧凑的两行单元格，移动端自适应显示，并在日期下方展示当天消费金额。
- **图表视觉优化**：分类占比、TOP 5 分类和支出趋势统一为更轻的分析卡片视觉，TOP 5 使用横向条形图。
- **版本号更新**：设置页版本号更新为 `v1.6.5`

### v1.6.4 (2026-07-01)

- **支出节奏**：新增时间进度与支出进度对比，帮助快速判断当前花费是否偏快。
- **日历热力图**：按日期展示支出强度，点击日期可跳到账单明细查看当天记录。
- **异常提醒**：自动展示高支出日、最大单笔和上涨最多的分类/标签。
- **全局筛选联动**：新增洞察模块继续遵循概览页顶部筛选。
- **版本号更新**：设置页版本号更新为 `v1.6.4`

### v1.6.3 (2026-06-30)

- **概览全局筛选**：顶部时间、标签、金额和搜索筛选统一作用于总额、分类占比、TOP 5 和支出趋势。
- **分组分析维度**：分类占比和 TOP 5 共用分析维度选择，可查看全部分组或聚焦消费类型、支付方式、人员、渠道等具体分组。
- **趋势统一时间口径**：支出趋势取消独立时间下拉，跟随顶部时间范围，长时间范围自动按月汇总。
- **筛选标签压缩显示**：整组全选时显示一个分组 chip，避免筛选栏被大量标签挤满。
- **主屏幕秒开优化**：已安装 PWA 现在优先从缓存打开账单页，并在后台刷新同源静态资源。
- **按需加载大库**：Chart.js 改为进入概览页时加载，SheetJS 改为 Excel 导入时加载，默认记账页不再等待这两个 CDN。
- **首屏初始化瘦身**：默认启动不再提前渲染概览图表和明细列表，进入对应页面时再加载。
- **静态资源缓存**：版本化 CSS/JS 使用长缓存，资源参数更新为 `v=165`。
- **版本号更新**：设置页版本号更新为 `v1.6.3`

### v1.6.2 (2026-06-29)

- **常用标签优先**：标签选择器按最近 90 天使用频率排序，常用标签自动排在同组前面。
- **已选标签置顶**：多选时已选标签在分组内保持靠前，方便确认和取消选择。
- **同日账单排序**：账单明细按时间倒序排序时，同一天内后添加的账单会显示在前面。
- **轻量实时统计**：排序直接从本地账单推导，不增加新的配置负担，导入和新增账单后会自然更新。
- **版本号更新**：设置页版本号更新为 `v1.6.2`

### v1.6.1 (2026-06-29)

- **移动端标签选择器**：记账和编辑页点击标签后优先打开底部多选面板，不再默认唤起键盘。
- **连续多选与分组折叠**：选择标签后面板保持打开，分组可折叠，标签多时更容易快速定位。
- **按分组新增标签**：每个分组内可直接新增标签，点击新增输入框时才唤起键盘，并自动同步到标签管理。
- **跨分组同名标签**：同一分组内仍避免重名，不同分组允许同名标签表达不同含义。
- **移动端输入优化**：移动端输入字号提升到 16px，减少 iOS 聚焦后页面自动放大和键盘遮挡问题。
- **版本号更新**：设置页版本号更新为 `v1.6.1`

### v1.6.0 (2026-06-26)

- **数据安全中心**：新增普通备份、自动保存到本地文件、恢复检查与备份提醒。
- **可选加密备份**：默认不加密，按需开启密码加密，恢复时自动识别并要求对应密码。
- **恢复更稳妥**：恢复前展示摘要，支持覆盖或合并，失败时尽量回滚到恢复前状态。
- **轻量体验**：高级功能默认收起，首页只在需要时轻提示，避免让日常记账变复杂。
- **版本号更新**：设置页版本号更新为 `v1.6.0`

### v1.5.3 (2026-06-18)

**问题反馈入口：**

- **设置页新增问题反馈模块**：独立的问题反馈入口，位于"危险操作"与"关于"之间
- **邮箱复制功能**：反馈邮箱地址 `billnest_feedback@outlook.com` 支持点击复制，方便用户在其他设备上手动发送邮件
- **一键发送邮件**：发送邮件按钮使用 `window.open` 打开，避免页面跳转
- **界面对齐**：反馈模块采用标准 setting-item 布局（标题"联系我们" + 描述），与其他设置项风格统一
- **版本号更新**：设置页版本号更新为 `v1.5.3`

### v1.5.2 (2026-06-17)

**移动端导航与导入刷新优化：**

- **记账入口优化**：底部“记账”按钮从突出的悬浮圆形 FAB 改为与其他 tab 一致的导航项，用主色图标块保持重点但降低突兀感
- **导入标签即时刷新**：文件导入创建新标签后，刷新内存标签状态、明细和概览；进入标签管理页时主动重新读取 IndexedDB，避免 PWA 主屏模式下必须刷新网页才能看到标签
- **主题设置迁移**：右上角改为设置入口，主题模式移入设置页，支持浅色、深色、跟随系统
- **导航结构调整**：底部保留概览、明细、记账、标签四项，标签管理从“更多”菜单中移出
- **版本号更新**：设置页版本号更新为 `v1.5.2`

### v1.5.0 (2026-06-15)

**UI 全面重构（温润的日式账本 — Forest Canopy 主题）：**

完整设计文档：`docs/expense-ui-optimization-plan.md`

- **仪表盘 Hero 重构**：4 个 stat-card 替换为大号金额 Hero 区域（本月支出 + 趋势对比 + 笔数），空状态引导
- **底部导航重构**：5 项精简为 4 项，移动端优先展示高频入口
- **色彩系统重定义**：Forest Canopy 温润墨绿主题，暖调米白背景，单 accent 色，删除多色 stat-card 配色
- **排版系统**：引入 Noto Serif SC（衬线标题）+ JetBrains Mono（等宽金额数字），金额 `tabular-nums` 对齐
- **去卡片化**：图表区域移除卡片外壳，改用分隔线和留白，减少视觉噪音
- **动效编排**：Staggered Reveal 入场动画（fadeUp），页面切换 fadeSlideIn，Modal 底部弹出，Toast 胶囊式
- **无障碍修复**：恢复 `user-scalable`，`prefers-reduced-motion` 支持，`color-scheme: dark`，`aria-label` 补充，`touch-action: manipulation`
- **安全区域适配**：刘海屏 `env(safe-area-inset-*)` 全面支持，底部导航 + body padding
- **移动端优化**：滚动收缩 Header，触控目标 ≥ 44px，`transition: all` 全部替换为明确属性
- **深色模式**：`#1A1D1B` 深墨绿底 + `#242826` 卡片，`color-scheme: dark` 系统级适配

### v1.4.2 (2026-06-15)

**Bug 修复（演示模式残留问题 - 补充修复）：**
- 修复首次进入后明细页数据为空的问题（根因修复）
  - 根因：`app.js` 模块级 `switchView` 覆盖在 `index.html` 内联 `switchView` 定义之前执行，`originalSwitchView` 捕获为 `undefined`，随后内联 `function switchView()` 声明覆盖了 `app.js` 的覆盖，导致导航按钮点击不触发 `renderExpenseList()`
  - 修复：将 `switchView` 覆盖从模块级移到 `DOMContentLoaded` 处理器内，确保在内联脚本定义 `switchView` 之后执行
- 修复标签页计数始终显示"0笔"的问题（根因修复）
  - 根因：`loadTags()` 中 `renderTagsList()` 未 await，首次调用（0 条数据）可能晚于第二次调用（20 条演示数据）完成，导致过期输出覆盖正确计数
  - 修复：`loadTags()` 中 `await renderTagsList()`
- 修复关闭演示模式后默认标签仍然存在的问题（根因修复）
  - 根因：同上 race condition，`disableDemoMode()` 后 `loadTags()` 调用 `renderTagsList()` 未 await，可能导致标签页渲染过期状态
  - 修复：同上
- 修复 `goToListFromDashboard` 双重渲染问题：使用 `_originalSwitchView` 而非 `switchView` 避免重复触发 `renderExpenseList`

### v1.4.1 (2026-06-15)

**Bug 修复（演示模式残留问题）：**
- 修复首次进入后明细页数据为空的问题
  - 根因：`enableDemoMode()` 清空重建 DB 后未刷新 `allTags` 内存状态，`renderExpenseList` 使用的旧 tag 对象与新 DB 数据不匹配
  - 修复：`enableDemoMode()` 后调用 `loadTags()` 刷新 `allTags`，并重渲染明细列表和概览页
- 修复标签页计数始终显示为 0 的问题
  - 根因同上：`allTags` 指向已被清空的旧 DB 对象，标签 ID 虽然相同但对象引用不匹配导致计数异常
  - 修复同上
- 修复关闭演示模式后默认标签仍然存在的问题
  - 根因：`enableDemoMode()` 备份了 `initDB()` 自动创建的默认标签，`disableDemoMode()` 恢复时将其写回 DB
  - 修复：`disableDemoMode()` 仅在备份中有实际 expense 数据时才恢复 tags/tagGroups，空 expense 时不恢复

### v1.4.0 (2026-06-14)

**Bug 修复（演示模式）：**
- 修复无痕模式首次进入时标签计数为 0、概览数量不符的问题
  - 根因：首次初始化使用 `generateTestData()`（60-180 条随机数据）而非 `enableDemoMode()`（固定 20 条样本数据），两条路径不一致
  - 修复：统一使用 `enableDemoMode()` 作为首次初始化入口
- 修复演示数据日期不在当前月份导致概览页显示 0 的问题
  - 修复：`DEMO_SAMPLE_DATA` 改为 `getDemoSampleData()` 函数，动态生成当前月份的日期
- 修复关闭演示模式后默认标签（支付方式、人员、消费类型等）残留的问题
  - 根因：`disableDemoMode()` 无备份时调用 `initDB()` 重新创建了默认标签
  - 修复：无备份时保持空数据库，不再调用 `initDB()`
- 修复演示模式下"加载更多"可以加载超过 20 条数据的问题
  - 根因：`generateTestData()` 生成 60-180 条随机数据，而 `getExpenses()` 无分页限制
  - 修复：统一使用 `enableDemoMode()` 的固定 20 条样本数据

### v1.3.8 (2026-06-13)

**PWA 图标修复：**
- 修复 iOS Safari 添加到主屏幕仍显示旧图标的问题
  - `apple-touch-icon` 引用从 `/icons/icon-*.png` 改为 `/icons/expense-icon-*.png`

### v1.3.7 (2026-06-13)

**PWA 图标：**
- 生活账单独立 PWA 图标更换为专属设计（绿色圆角方形背景 + 钱袋图标）
  - 生成 72x72 / 96x96 / 128x128 / 144x144 / 152x152 / 192x192 / 384x384 / 512x512 全尺寸图标
  - manifest.json 引用新的 `expense-icon-*.png` 系列图标

### v1.3.6 (2026-06-13)

**PWA 支持：**
- 生活账单支持独立添加到手机主屏幕（像原生 App 一样使用）
  - 新增独立的 `manifest.json`（`scope: /tools/expense/`，`start_url: /tools/expense/`）
  - 新增独立的 Service Worker（`tools/expense/sw.js`），专门缓存生活账单资源
  - 页面加载时自动注销主站 SW（避免冲突），注册独立 SW
  - 标题改为"生活账单 - Expense Tracker"，更符合独立应用身份

### v1.3.5 (2026-06-13)

**Bug 修复：**
- 修复演示模式关闭后自定义数据完全丢失的问题
  - 根因：`enableDemoMode()` 将备份保存在 IndexedDB settings store 中，但 `clearAllData()` 清空了所有 store，导致备份被一并清除
  - 修复：备份机制改为 `localStorage`，不受 `clearAllData()` 影响
- 修复演示模式关闭时 `importData` 调用错误的问题
  - 根因：`app.js` 的 `window.importData`（文件导入）覆盖了 `db.js` 的 `importData`（数据对象导入），`guide.js` 调用时传入普通对象但执行了文件导入逻辑
  - 修复：`app.js` 的 `window.importData` 添加参数类型检测，数据对象路由到 `db.js` 版本，文件输入路由到 UI 版本

### v1.3.4 (2026-06-13)

**Bug 修复：**
- 修复演示模式切换时自定义 tag groups 丢失的问题
  - `enableDemoMode()` 备份时现在包含 `tagGroups`
  - `disableDemoMode()` 恢复时现在恢复 `tagGroups`
  - 恢复条件从 `backup.expenses && backup.tags` 改为 `backup.expenses || backup.tags || backup.tagGroups`，支持只有 tag groups 的场景

### v1.3.3 (2026-06-13)

**移动端体验优化：**
- 修复概览页已选标签删除按钮变形问题（移除 font-size/font-weight 文字样式，添加 min/max 约束确保正圆）

### v1.3.2 (2026-06-13)

**移动端体验优化：**
- 修复概览页标签弹框关闭按钮背景变形为椭圆的问题（固定宽高+min/max约束）
- 标签管理页标签项改为两行布局：第一行显示标签名+笔数，第二行显示操作按钮
- 标签名过长时自动省略显示（桌面端200px，移动端140px）

### v1.3.1 (2026-06-13)

**移动端体验优化：**
- 概览页标签弹框关闭按钮改用 SVG 图标，避免中文叉号变形
- 概览页已选标签删除按钮改用 SVG 图标，避免中文叉号变形
- 标签管理页操作按钮改为图标按钮（重命名/移动/合并/删除），节省空间不拥挤
- 分组操作按钮同样改为图标按钮

### v1.3.0 (2026-06-12)

**Bug 修复与体验优化：**
- 修复演示模式首次开启无数据问题（generateTestData 失败时不标记已初始化）
- 修复数据导入确认后无反应问题（pendingImportRecords 跨文件变量同步）
- 修复记账页日期输入框移动端布局凸出（max-width: 100%）
- 自然语言输入框重构：增大输入区（rows=5, min-height:100px），字符掩码法精确解析，解析结果改为可编辑表单
- 表单字段顺序调整：日期→项目→金额→标签，符合用户输入习惯
- 修复账单明细全部分类下拉框空白问题（同时匹配 category 和 tags 数组）
- 标签管理页输入框布局优化，按钮不再换行
- 替换 prompt() 系统弹窗为自定义重命名弹框
- 概览页标签弹窗移动端优化：增大关闭按钮、缩小已选标签芯片、底部抽屉式动画
- 修复新建分组无反应问题（增加错误处理）
- 趋势图支持本月/上月/近7天时间范围切换
- 全局二级弹框（合并标签、删除确认等）样式优化

---

### v1.2.0 (2026-06-11)

**核心新功能：标签层级体系（一级分组 + 二级标签）**
- 默认预设分组：支付方式、人员、消费类型、渠道、未分类
- 标签管理页面完全重构为树状结构：可折叠分组卡片，展开/收起分组
- 支持新建分组、重命名分组、删除分组（删除分组时子标签自动移到未分类）
- 支持在不同分组之间移动标签，自定义模态框选择目标分组
- 筛选弹窗重构：按分组分类展示标签，点击分组标题一键全选/取消全选
- 选中标签后筛选栏显示「分组 · 标签」格式方便识别
- 饼图新增「按分组聚合」切换：点击标题"按标签"/"按分组"可以切换聚合方式
- 智能标签建议列表中显示分组名称，方便快速识别
- **数据库升级：DB_VERSION 从 1 → 2，自动迁移原有标签到消费类型分组**

**体验优化：**
- 通用自定义模态框支持任意 HTML 内容，复用度更高
- 移动端适配树状布局缩进和分组操作按钮
- 分组显示标签个数和总支出笔数统计

---

### v1.1.0 (2026-06-11)

**新增功能：**
- 数据导入完整预览：全量可滚动列表，所有字段支持内联编辑（日期/金额/项目/标签）
- 导入记录逐行删除 + 一键批量删除无效行
- 重新验证按钮：编辑后实时刷新记录状态
- 独立子站点访问：`/tools/expense/` 可直接访问，无需从主页进入
- PWA 完整支持：manifest、iOS 图标、pwa.css/js 均可在子页面独立加载
- 绝对路径引用：带/不带尾部斜杠的 URL 均可正确加载资源
- 自定义确认弹窗（替换原生 confirm/alert）
- 新用户引导只显示一次（同一设备）

**问题修复：**
- 修复 Service Worker cache-first 导致手机端加载旧版 CSS
- 修复清除数据后演示数据未同步标记导致开关状态不一致
- 修复首次访问时"演示模式"开关与实际状态不匹配
- 修复标签弹窗在手机端位置遮挡
- 修复筛选栏金额范围输入框不对齐

---

### v1.0.0 (2026-05)

**初始版本：**
- 概览面板：统计卡片 + Chart.js 图表（饼图、柱状图、折线图）
- 快捷记账：表单录入 + 自然语言解析
- 账单明细：日期分组、搜索/分类/排序筛选
- 标签管理：CRUD、颜色自定义、合并、按标签筛选
- 数据导入导出：CSV/Excel 导入、CSV/JSON 导出
- 演示模式 + 测试数据生成
- 深色主题切换
- PWA 基础支持
- 移动端响应式布局
